import NotesApp from "@/components/NotesApp";

export const metadata = {
  title: "My Notes | AI Study Assistant",
  description: "Create, view, and delete study notes.",
};

export default function NotesPage() {
  return <NotesApp />;
}
